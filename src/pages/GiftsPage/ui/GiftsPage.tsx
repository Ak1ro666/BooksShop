import { LayoutProvider } from '@/app/providers/LayoutProvider';
import { FC } from 'react';

const GiftsPage: FC = () => {
	return (
		<LayoutProvider>
			<div>GiftsPage</div>
		</LayoutProvider>
	);
};

export default GiftsPage;

